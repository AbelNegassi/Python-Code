# Write a program to promt the user for hours and rate
# per hour to compute gross pay

#Prompt user for input
hours = input("Enter Hours: ")
rate = input("Enter Rate: ")

# Convert strings to numbers
hours = float(hours)
rate = float(rate)

#overtime pay
if hours >= 40:
    rate = rate*1.5
    
#Calculate pay
pay = hours*rate
print(f"Your pay is ${pay}.")

